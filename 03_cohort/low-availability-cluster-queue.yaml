---
apiVersion: kueue.x-k8s.io/v1beta1
kind: ClusterQueue
metadata:
  name: low-availability-cluster-queue
spec:
  namespaceSelector: {} # Available to all namespaces
  cohort: jobs-cohort
  queueingStrategy: BestEffortFIFO # Default queueing strategy
  resourceGroups:
    - coveredResources: ["cpu", "memory"]
      flavors:
        - name: "default-flavor"
          resources:
            - name: "cpu"
              nominalQuota: "2"
              lendingLimit: "1"
            - name: "memory"
              nominalQuota: 2Gi
              lendingLimit: "1Gi"
